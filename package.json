{
  "name": "{{MODULE_ID}}",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "{{MODULE_DESCRIPTION}}",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "vite build",
    "clean": "rimraf dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "link:foundry": "node scripts/link-to-foundry.mjs",
    "init:template": "node scripts/init-template.mjs",
    "prepare:manifest": "node scripts/prepare-release-manifest.mjs",
    "mcp:build": "tsc -p tools/mcp/tsconfig.json",
    "mcp:watch": "tsc -p tools/mcp/tsconfig.json --watch"
  },
  "devDependencies": {
    "@league-of-foundry-developers/foundry-vtt-types": "github:League-of-Foundry-Developers/foundry-vtt-types#main",
    "@modelcontextprotocol/sdk": "^1.0.4",
    "@types/node": "^22.10.0",
    "rimraf": "^6.0.1",
    "sass-embedded": "^1.99.0",
    "tsx": "^4.19.2",
    "typescript": "^5.9.3",
    "vite": "^8.0.0",
    "vite-plugin-static-copy": "^4.1.0",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20.11.0"
  }
}
